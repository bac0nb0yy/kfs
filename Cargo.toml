[package]
name = "kfs"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "z"
panic = "abort"

[lib]
path = "src/rust/lib.rs"
crate-type = ["staticlib"]
test = false
doctest = false
bench = false
